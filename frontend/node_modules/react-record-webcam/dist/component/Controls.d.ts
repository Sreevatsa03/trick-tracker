import React from 'react';
import { CAMERA_STATUS } from '../constants';
declare type ControlsProps = {
    openCamera(): void;
    closeCamera(): void;
    start(): void;
    stop(): void;
    retake(): void;
    download(): void;
    getRecording(): void;
    status: keyof typeof CAMERA_STATUS;
    cssNamespace?: string;
    labels?: {
        CLOSE: string | number;
        DOWNLOAD: string | number;
        OPEN: string | number;
        RETAKE: string | number;
        START: string | number;
        STOP: string | number;
    };
};
export declare const Controls: (props: ControlsProps) => React.ReactElement;
export {};
